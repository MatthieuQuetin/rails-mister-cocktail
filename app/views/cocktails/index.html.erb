<div class="container">
  <div class="row">
    <div class="col-sm">
      <h1>COCKTAILS</h1>
        <ul>
          <% @cocktails.each do |cocktail| %>
          <li> <%= link_to cocktail.name, cocktail_path(cocktail) %> </li>
          <% end  %>
        </ul>
    </div>
  </div>
</div>
